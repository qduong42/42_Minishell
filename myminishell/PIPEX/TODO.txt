- ADDING the correct behavior for symbolic links

int	fucked_cat(t_pipe *cmd)
{
	int	i;

	i = 1;
	if (!ft_strncmp(cmd->argv[0], "cat", ft_strlen(cmd->argv[0])))
	{
		while (cmd->argv[i])
		{
			if (ft_strncmp(cmd->argv[i], "-e", 2))
				break ;
			i++;
		}
		if (cmd->argv[i] || cmd->fd_in > 0)
			return (0);
		return (1);
	}
	return (0);
}